# Project

- Maintain a standalone CLI that mirrors ChatGPT history, with documentation tying `Fish Spine Symbolism` updates to the local skill context.
- Ensure tooling can consume the data-layer endpoints (not just local SQLite artifacts) so we can self-refresh without manual export.
- Current focus: the CLI, the `scripts/view_conversation.py` helper, and the `chat-context-sync` documentation workflows.
