The equality audit shows the likely source is not the final scaling theorem: the current canonical Stage 3 witness is saturated before scaling. I’m testing that directly at the unit-energy carrier by proving the canonical weak error evaluates to the same unit energy as the strong norm; if `refl` closes, the source is formally identified as the coarse quadratic-form witness, not an unresolved arithmetic step.